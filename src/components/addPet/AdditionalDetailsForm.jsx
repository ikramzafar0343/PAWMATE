import React from 'react';

const AdditionalDetailsForm = ({ formData, handleInputChange, showAdditional, setShowAdditional }) => {
  // This form has been removed - fields are no longer needed
  return null;
};

export default AdditionalDetailsForm;
