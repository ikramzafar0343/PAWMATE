import React from 'react';

const MedicalHistoryForm = ({ formData, handleInputChange }) => {
  // This form has been removed - fields are no longer needed
  return null;
};

export default MedicalHistoryForm;
